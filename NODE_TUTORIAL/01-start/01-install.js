/*
브라켓 (bracket)
[대괄호],{중괄호}
1.return :scope(중괄호) 안에서 제일 마지막 줄에 사용
    -프로그램이나 문법 끝났을 때 ex ) return;
    -반환값이 있을 때 ex) return a + b;  return "Welcome";     

2. File I/O => 예외처리(에러처리)
    -file.write -> error

3. Node JS : 중간 평가(nodejs + json + file i/o) + 최종(express + mysql)

" " => "''"
'' => '" "'
console.log("busanit"+변수);
console.log(`busanit ${변수}`);
*/
