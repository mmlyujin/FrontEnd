/*
import 가져와서 사용할 프로그램/파일이 있는지....
import 변수명 from "파일이름"

function : 파일이름과 동일한 함수 생성
  return(
    <div>
      작업코드
    </div>
  );

export default 파일이름
*/
import axios from "axios";

function App() {
  return(
    <div>
      작업코드
    </div>
  );
}

export default App;